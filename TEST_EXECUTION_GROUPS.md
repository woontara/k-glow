# K-Glow 테스트 실행 그룹화 전략

## 병렬 실행 그룹 분류

테스트를 의존성에 따라 8개 그룹으로 분류하여 최대 병렬 실행 효율을 달성합니다.

---

## 🟢 Group 1: 독립 UI 테스트 (병렬 실행 가능)

**특징**:
- 로그인 불필요
- DB 상태 변경 없음
- 읽기 전용 테스트
- 완전히 독립적으로 실행 가능

**시나리오** (14개):

### 홈페이지
- ✅ 1.1 네비게이션 메뉴 - 데스크톱
- ✅ 1.2 모바일 네비게이션
- ✅ 2.1 홈페이지 레이아웃
- ✅ 2.2 기능 카드 링크
- ✅ 2.3 CTA 버튼

### 브랜드 분석 (UI만)
- ✅ 6.1 페이지 로드
- ✅ 6.2 URL 미입력 검증
- ✅ 6.3 크롤링 깊이 조정

### 견적 계산기 (UI만)
- ✅ 5.1 페이지 로드
- ✅ 5.7 필수 항목 검증

### 반응형 테스트
- ✅ 9.1 모바일 (375x667)
- ✅ 9.2 태블릿 (768x1024)
- ✅ 9.3 데스크톱 (1920x1080)

### 성능 테스트
- ✅ 10.1 페이지 로드 성능

**Playwright 설정**:
```typescript
// tests/group1-independent-ui.spec.ts
test.describe.configure({ mode: 'parallel' });

test.describe('Group 1: 독립 UI 테스트', () => {
  // 모든 테스트 병렬 실행
});
```

---

## 🔵 Group 2: 파트너사 읽기 테스트 (병렬 실행 가능)

**특징**:
- 기존 파트너사 데이터 조회만
- DB 변경 없음
- 로그인 불필요 (공개 페이지)
- 같은 데이터를 읽지만 다른 기능 테스트

**사전 조건**:
- 최소 5개 파트너사 데이터 존재
- 각 파트너사에 제품 3개 이상

**시나리오** (5개):

### 파트너사 관리
- ✅ 4.1 파트너사 목록 표시
- ✅ 4.2 파트너사 검색
- ✅ 4.3 파트너사 상세 페이지
- ✅ 4.4 빈 상태 (별도 DB 필요)
- ✅ 4.5 외부 링크

**Playwright 설정**:
```typescript
// tests/group2-partners-read.spec.ts
test.describe.configure({ mode: 'parallel' });

test.beforeAll(async () => {
  // 테스트 데이터 확인 (최소 5개 파트너사)
});

test.describe('Group 2: 파트너사 읽기', () => {
  // 병렬 실행 가능
});
```

---

## 🟡 Group 3: 견적 계산 기능 테스트 (병렬 실행 가능)

**특징**:
- DB 변경 없음 (계산만 수행)
- 로그인 불필요
- 각 테스트가 독립적인 계산 수행
- API 호출만 발생

**시나리오** (7개):

### 견적 계산기
- ✅ 5.2 단일 제품 계산
- ✅ 5.3 다중 제품 추가
- ✅ 5.4 제품 삭제
- ✅ 5.5 배송 방법 변경
- ✅ 5.6 인증 비용 계산
- ✅ 5.8 환율 실패 처리
- ✅ 5.9 PDF 다운로드 (준비 중)

**Playwright 설정**:
```typescript
// tests/group3-calculator.spec.ts
test.describe.configure({ mode: 'parallel' });

test.describe('Group 3: 견적 계산', () => {
  // 각 테스트는 독립적인 계산 수행
  // API Mock 사용 권장
});
```

---

## 🟣 Group 4: 회원가입 및 인증 테스트 (순차 실행)

**특징**:
- DB에 사용자 생성
- 이메일 중복 검증 필요
- 순차적으로 실행해야 상태 일관성 유지

**의존성**:
- 3.1 → 3.4 (회원가입 후 중복 이메일 테스트)
- 3.5 → 3.6, 3.7 (로그인 성공 후 실패 케이스)

**시나리오** (7개):

### 인증 - 회원가입
- 🔴 3.1 회원가입 - BRAND (정상) ← 먼저 실행
- 🔴 3.4 회원가입 - 중복 이메일 ← 3.1 의존

### 인증 - 회원가입 검증 (병렬 가능)
- ✅ 3.2 회원가입 - 비밀번호 불일치
- ✅ 3.3 회원가입 - 짧은 비밀번호

### 인증 - 로그인
- 🔴 3.5 로그인 - 정상 플로우 ← 먼저 실행
- ✅ 3.6 로그인 - 잘못된 비밀번호 (병렬)
- ✅ 3.7 로그인 - 존재하지 않는 계정 (병렬)

**Playwright 설정**:
```typescript
// tests/group4-auth-signup.spec.ts
test.describe.configure({ mode: 'serial' }); // 순차 실행

let newUserEmail: string;

test.describe('Group 4: 회원가입 및 인증', () => {
  test('3.1 회원가입 성공', async ({ page }) => {
    newUserEmail = `test-${Date.now()}@example.com`;
    // 회원가입
  });

  test('3.4 중복 이메일 거부', async ({ page }) => {
    // newUserEmail 재사용
  });
});

// 검증 테스트는 별도 병렬 그룹
test.describe.configure({ mode: 'parallel' });
test.describe('회원가입 검증', () => {
  test('3.2 비밀번호 불일치', () => {});
  test('3.3 짧은 비밀번호', () => {});
});
```

---

## 🟠 Group 5: 로그인 상태 테스트 - BRAND (병렬 실행 가능)

**특징**:
- BRAND 계정으로 로그인 필요
- DB 읽기만 수행
- 각 테스트는 독립적인 세션 사용

**사전 조건**:
- BRAND 계정 존재: brand@test.com

**시나리오** (6개):

### 네비게이션 - 로그인 후
- ✅ 1.3 로그인 전 네비게이션
- ✅ 1.4 로그인 후 네비게이션 (BRAND)
- ✅ 3.8 로그아웃
- ✅ 3.9 미인증 리다이렉트

### 인증 대행 - 읽기
- ✅ 7.1 로그인 필수
- ✅ 7.2 인증 신청 페이지 로드

**Playwright 설정**:
```typescript
// tests/group5-brand-session.spec.ts
test.describe.configure({ mode: 'parallel' });

test.describe('Group 5: BRAND 세션', () => {
  test.beforeEach(async ({ page }) => {
    // 각 테스트마다 새로운 로그인 세션
    await page.goto('/auth/signin');
    await page.fill('input[type="email"]', 'brand@test.com');
    await page.fill('input[type="password"]', 'brand123!@#');
    await page.click('button[type="submit"]');
  });

  // 각 테스트 병렬 실행
});
```

---

## 🔴 Group 6: 인증 대행 신청 테스트 (순차 실행)

**특징**:
- DB에 인증 신청 생성
- 파일 업로드 포함
- 상태 변경 (PENDING → IN_PROGRESS 등)
- 순차적으로 실행해야 상태 일관성 유지

**의존성**:
- 7.8 (신청 생성) → 7.10 (목록 조회) → 7.11 (필터링) → 7.13 (상세 조회)

**사전 조건**:
- BRAND 계정 로그인
- 최소 1개 파트너사 존재

**시나리오** (11개):

### 인증 신청 - UI 검증 (병렬 가능)
- ✅ 7.3 파트너사 로드
- ✅ 7.4 파트너사 없을 때
- ✅ 7.5 인증 종류 선택
- ✅ 7.9 필수 항목 미입력

### 인증 신청 - 파일 업로드 (병렬 가능)
- ✅ 7.6 파일 업로드
- ✅ 7.7 파일 삭제

### 인증 신청 - 데이터 플로우 (순차)
- 🔴 7.8 정상 제출 ← 먼저 실행 (DB에 데이터 생성)
- 🔴 7.10 인증 현황 - 목록 ← 7.8 의존
- 🔴 7.11 필터링 ← 7.8 의존
- 🔴 7.13 상세 페이지 ← 7.8 의존
- ✅ 7.12 빈 상태 (별도 세션)

**Playwright 설정**:
```typescript
// tests/group6-certification.spec.ts

// UI 검증 - 병렬
test.describe.configure({ mode: 'parallel' });
test.describe('인증 UI 검증', () => {
  test('7.3 파트너사 로드', () => {});
  test('7.5 인증 종류', () => {});
  test('7.9 필수 항목', () => {});
});

// 파일 업로드 - 병렬
test.describe('파일 업로드', () => {
  test('7.6 파일 업로드', () => {});
  test('7.7 파일 삭제', () => {});
});

// 데이터 플로우 - 순차
test.describe.configure({ mode: 'serial' });
let certificationId: string;

test.describe('인증 신청 플로우', () => {
  test('7.8 신청 제출', async ({ page }) => {
    // 인증 신청 생성
    certificationId = '...';
  });

  test('7.10 목록 조회', async ({ page }) => {
    // 7.8에서 생성된 인증 확인
  });

  test('7.11 필터링', async ({ page }) => {
    // 생성된 데이터로 필터 테스트
  });

  test('7.13 상세 조회', async ({ page }) => {
    await page.goto(`/certification/${certificationId}`);
  });
});
```

---

## 🟤 Group 7: 관리자 전용 테스트 (순차 실행)

**특징**:
- ADMIN 계정 로그인 필요
- 인증 상태 변경 (DB 수정)
- 다른 사용자 데이터 조회/수정

**의존성**:
- Group 6의 데이터 필요 (인증 신청 존재)
- 8.3 (상태 변경)은 8.2 (목록 조회) 후 실행

**사전 조건**:
- ADMIN 계정: admin@k-glow.com
- 인증 신청 데이터 존재 (Group 6 실행 후)

**시나리오** (4개):

### 관리자 기능
- ✅ 1.5 ADMIN 사용자 네비게이션
- ✅ 3.10 ADMIN 접근 제어
- 🔴 8.1 접근 권한
- 🔴 8.2 인증 관리 목록
- 🔴 8.3 상태 변경 ← 8.2 의존

**Playwright 설정**:
```typescript
// tests/group7-admin.spec.ts
test.describe.configure({ mode: 'serial' });

test.describe('Group 7: 관리자 기능', () => {
  test.beforeAll(async ({ browser }) => {
    // ADMIN 로그인
  });

  test('8.1 접근 권한', () => {});
  test('8.2 목록 조회', () => {});
  test('8.3 상태 변경', () => {
    // 8.2에서 조회한 인증의 상태 변경
  });
});
```

---

## ⚫ Group 8: 브랜드 분석 및 통합 테스트 (순차 실행)

**특징**:
- DB에 파트너사 생성
- API Mock 사용
- 생성 → 조회 플로우 테스트

**의존성**:
- 6.4 (분석 완료) → 4.1 (파트너사 목록에서 확인)
- 6.6 (DB 저장 옵션)은 6.4와 독립적

**시나리오** (3개):

### 브랜드 분석
- 🔴 6.4 분석 정상 플로우 (Mock) ← DB에 파트너사 생성
- ✅ 6.5 분석 실패 처리 (병렬)
- ✅ 6.6 DB 저장 옵션 (병렬)

**Playwright 설정**:
```typescript
// tests/group8-brand-analysis.spec.ts
test.describe.configure({ mode: 'serial' });

test.describe('브랜드 분석 통합', () => {
  test('6.4 분석 성공 및 DB 저장', async ({ page }) => {
    // Mock API
    // 파트너사 생성 확인
  });

  test('파트너사 목록에서 확인', async ({ page }) => {
    await page.goto('/partners');
    // 6.4에서 생성된 파트너사 존재 확인
  });
});

// 오류 케이스는 병렬
test.describe.configure({ mode: 'parallel' });
test.describe('브랜드 분석 오류', () => {
  test('6.5 실패 처리', () => {});
  test('6.6 DB 저장 옵션', () => {});
});
```

---

## 🔐 Group 9: 보안 및 성능 테스트 (병렬 실행 가능)

**특징**:
- DB 변경 없음
- 공격 시뮬레이션
- 성능 측정
- 완전 독립적

**시나리오** (7개):

### 보안
- ✅ 11.1 XSS 방지
- ✅ 11.2 SQL Injection 방지
- ✅ 11.3 파일 크기 제한
- ✅ 11.4 중복 제출 방지

### 성능 및 접근성
- ✅ 10.2 이미지 오류 처리
- ✅ 10.3 키보드 네비게이션
- ✅ 10.4 ARIA 속성
- ✅ 10.5 네트워크 오류 복구

**Playwright 설정**:
```typescript
// tests/group9-security-performance.spec.ts
test.describe.configure({ mode: 'parallel' });

test.describe('Group 9: 보안 및 성능', () => {
  // 모든 테스트 병렬 실행 가능
});
```

---

## 🌐 Group 10: 크로스 브라우저 테스트 (병렬 실행 가능)

**특징**:
- 다른 브라우저에서 동일 테스트
- DB 변경 없음
- 주요 기능만 샘플링

**시나리오** (2개):

### 크로스 브라우저
- ✅ 9.4 Firefox
- ✅ 9.5 Safari (WebKit)

**Playwright 설정**:
```typescript
// playwright.config.ts
export default defineConfig({
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'firefox', use: { ...devices['Desktop Firefox'] } },
    { name: 'webkit', use: { ...devices['Desktop Safari'] } },
  ],
});

// 그룹 1-9의 주요 테스트를 각 브라우저에서 실행
```

---

## 📊 실행 전략 요약

### 최대 병렬 실행 (빠른 피드백)

```bash
# 모든 병렬 그룹 동시 실행
npx playwright test tests/group1-independent-ui.spec.ts \
                     tests/group2-partners-read.spec.ts \
                     tests/group3-calculator.spec.ts \
                     tests/group5-brand-session.spec.ts \
                     tests/group9-security-performance.spec.ts \
                     --workers=5
```

### 순차 그룹 실행 (데이터 일관성 보장)

```bash
# 순차 그룹은 따로 실행
npx playwright test tests/group4-auth-signup.spec.ts --workers=1
npx playwright test tests/group6-certification.spec.ts --workers=1
npx playwright test tests/group7-admin.spec.ts --workers=1
npx playwright test tests/group8-brand-analysis.spec.ts --workers=1
```

### CI/CD 파이프라인 단계

```yaml
# .github/workflows/test.yml
jobs:
  test-parallel:
    name: 병렬 테스트
    strategy:
      matrix:
        group: [1, 2, 3, 5, 9]
    runs-on: ubuntu-latest
    steps:
      - run: npx playwright test tests/group${{ matrix.group }}-*.spec.ts

  test-sequential:
    name: 순차 테스트
    needs: test-parallel
    runs-on: ubuntu-latest
    steps:
      - run: npx playwright test tests/group4-auth-signup.spec.ts --workers=1
      - run: npx playwright test tests/group6-certification.spec.ts --workers=1
      - run: npx playwright test tests/group7-admin.spec.ts --workers=1
      - run: npx playwright test tests/group8-brand-analysis.spec.ts --workers=1
```

---

## 🎯 그룹별 실행 시간 예상

| 그룹 | 시나리오 수 | 실행 방식 | 예상 시간 | 병렬 효율 |
|------|------------|----------|-----------|----------|
| Group 1 | 14 | 병렬 | 30초 | 높음 ⚡⚡⚡ |
| Group 2 | 5 | 병렬 | 15초 | 높음 ⚡⚡⚡ |
| Group 3 | 7 | 병렬 | 20초 | 높음 ⚡⚡⚡ |
| Group 4 | 7 | 순차 | 45초 | 낮음 🐌 |
| Group 5 | 6 | 병렬 | 25초 | 높음 ⚡⚡⚡ |
| Group 6 | 11 | 혼합* | 60초 | 중간 ⚡⚡ |
| Group 7 | 4 | 순차 | 30초 | 낮음 🐌 |
| Group 8 | 3 | 혼합* | 40초 | 중간 ⚡⚡ |
| Group 9 | 7 | 병렬 | 25초 | 높음 ⚡⚡⚡ |
| Group 10 | 2 | 병렬 | 10초 | 높음 ⚡⚡⚡ |

*혼합: 일부 순차 + 일부 병렬

**총 예상 시간**:
- 순차 실행: ~5분
- 최적 병렬 실행: ~90초 (약 70% 개선)

---

## ⚠️ 주의사항

### 1. 테스트 격리 (Isolation)

```typescript
// 각 테스트는 독립적인 브라우저 컨텍스트 사용
test.use({
  storageState: undefined, // 세션 공유 방지
});

// 또는 각 테스트마다 새로운 컨텍스트
test.beforeEach(async ({ context }) => {
  await context.clearCookies();
});
```

### 2. 데이터베이스 격리

```typescript
// 테스트용 고유 데이터 생성
const testEmail = `test-${Date.now()}-${Math.random()}@example.com`;

// 또는 테스트 후 정리
test.afterEach(async () => {
  // 생성된 데이터 삭제 (선택적)
});
```

### 3. API Mock 사용 권장

```typescript
// 외부 API 의존성 제거
test.beforeEach(async ({ page }) => {
  await page.route('**/api/exchange-rate', route => {
    route.fulfill({
      status: 200,
      body: JSON.stringify({ krwToRub: 0.075 }),
    });
  });
});
```

### 4. 플래키 테스트 방지

```typescript
// 명시적 대기 사용
await page.waitForLoadState('networkidle');
await expect(element).toBeVisible({ timeout: 10000 });

// 조건부 대기
await page.waitForFunction(() => document.readyState === 'complete');
```

---

## 📁 권장 파일 구조

```
tests/
├── group1-independent-ui.spec.ts       # 병렬 ⚡⚡⚡
├── group2-partners-read.spec.ts        # 병렬 ⚡⚡⚡
├── group3-calculator.spec.ts           # 병렬 ⚡⚡⚡
├── group4-auth-signup.spec.ts          # 순차 🐌
├── group5-brand-session.spec.ts        # 병렬 ⚡⚡⚡
├── group6-certification.spec.ts        # 혼합 ⚡⚡
├── group7-admin.spec.ts                # 순차 🐌
├── group8-brand-analysis.spec.ts       # 혼합 ⚡⚡
├── group9-security-performance.spec.ts # 병렬 ⚡⚡⚡
├── group10-cross-browser.spec.ts       # 병렬 ⚡⚡⚡
├── fixtures/
│   ├── auth.ts                         # 로그인 헬퍼
│   ├── test-data.ts                    # 테스트 데이터 생성
│   └── api-mocks.ts                    # API Mock 설정
└── utils/
    ├── db-helpers.ts                   # DB 초기화/정리
    └── wait-helpers.ts                 # 대기 유틸리티
```

---

## 🚀 빠른 시작

### 1. 병렬 그룹만 실행 (빠른 검증)

```bash
npm run test:parallel
```

package.json:
```json
{
  "scripts": {
    "test:parallel": "playwright test tests/group{1,2,3,5,9}*.spec.ts --workers=5"
  }
}
```

### 2. 전체 테스트 스위트 실행

```bash
npm run test:all
```

package.json:
```json
{
  "scripts": {
    "test:all": "npm run test:parallel && npm run test:sequential",
    "test:sequential": "playwright test tests/group{4,6,7,8}*.spec.ts --workers=1"
  }
}
```

### 3. 특정 그룹만 실행

```bash
npx playwright test tests/group3-calculator.spec.ts
```

---

**작성일**: 2026-01-03
**버전**: 1.0
**최적화 목표**: 70% 실행 시간 단축 (5분 → 90초)
